import React from 'react';

const styles = {
  container: {
    width: '100%',
    height: '100vh',
    backgroundColor: 'lightsteelblue',
    display: 'flex',
    flexDirection: 'column',
  },
};

const Auth = () => {
  return (
    <div style={styles.container}>
      yoo
    </div>
  );
};

export default Auth;
